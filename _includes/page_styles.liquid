<style>
  /* M.S. thesis line: smaller, lighter, no quotes */
  .thesis {
    font-size: 0.82em;
    color: var(--global-text-color-light);
  }

  /* Cap profile photo height so it stays within the greeting section */
  .profile img,
  .profile-img,
  .clickable {
    max-height: 320px !important;
    width: auto !important;
    margin-left: auto;
    display: block;
  }

  /* Section headers (Education, News) — accent color */
  .home-section-header {
    font-weight: 700;
    margin-top: 1.5rem !important;
    margin-bottom: 1.6rem;
    font-size: 1.4rem;
    letter-spacing: -0.01em;
    color: var(--global-theme-color);
  }

  /* Date column (odd cells) of Education + News — accent color */
  .info-grid > div:nth-child(odd) {
    color: var(--global-theme-color);
  }

  /* Thinner divider with breathing room above */
  hr.section-divider {
    margin-top: 5rem;
    margin-bottom: 0;
    border: 0;
    border-top: 1px solid currentColor;
    opacity: 0.25;
  }

  /* Date-aligned grid for Education + News */
  .info-grid {
    display: grid;
    grid-template-columns: max-content 1fr;
    column-gap: 2em;
    row-gap: 1.4em;
    margin-top: 0.6em;
  }

  /* "See all" link below truncated News */
  .more-link {
    margin-top: 1.8rem;
    font-size: 0.95rem;
  }
</style>
